fastapi==0.116.1
uvicorn[standard]==0.35.0
pydantic==2.11.7
psycopg[binary]==3.2.9
python-dotenv==1.1.1
pypdf==5.9.0
httpx==0.28.1

# Local embeddings (sentence-transformers + multilingual-e5-small)
#
# IMPORTANT (build reliability on x86_64 + python:3.12-slim):
# Use the official PyTorch CPU wheel index to avoid accidentally pulling
# the *very large* PyPI "torch" wheel variant.
#
# If your network cannot reach download.pytorch.org, you can remove the
# extra-index line and fall back to PyPI (slower / larger download).
#
# We also pin the primary index explicitly to avoid pip preferring packages
# from the PyTorch index when the same version exists on PyPI.
--index-url https://pypi.org/simple
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.7.1+cpu
sentence-transformers==5.0.0
transformers==4.56.2
huggingface-hub==0.34.4
numpy==2.1.3

# Optional: keep a lightweight fallback available
# localhash is built-in and needs no deps.
